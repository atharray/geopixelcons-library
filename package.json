{
  "name": "geopixelcons-library",
  "private": true,
  "version": "1.0.2",
  "description": "Versioned GeoPixelcons++ feature library bundle",
  "scripts": {
    "build": "node build.js",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run build && npm test"
  },
  "devDependencies": {
    "terser": "^5.39.0"
  }
}
