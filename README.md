# GeoPixelcons Library

Versioned, readable `@require` bundle for GeoPixelcons++. This repository owns
portable feature controllers and their public contract; GeoPixelcons++ owns the
userscript metadata, settings, private state, native submission boundary, and
the small boot/fallback shell.

See [ARCHITECTURE.md](ARCHITECTURE.md) for the release-safe split design.
